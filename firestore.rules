rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Settings - single document
    match /settings/user {
      allow read, write: if request.auth != null;
    }

    // Meals collection
    match /meals/{mealId} {
      allow read, write: if request.auth != null;
    }

    // Daily exercise tracking
    match /exercise/{dateId} {
      allow read, write: if request.auth != null;
    }
  }
}
