rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /meal-images/{imageId} {
      allow read: if true; // Single user app
      allow write: if true && request.resource.size < 5 * 1024 * 1024; // 5MB max
    }
  }
}
